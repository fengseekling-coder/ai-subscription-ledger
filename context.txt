# 订阅账本 — 开发约定

## Tauri 开发环境启停

- **启动**：统一使用 `npm run tauri dev`（在 `apps/desktop` 目录下），会同时拉起 Vite 前端与 Tauri 桌面进程。
- **停止**：要停就整套一起停，不要只关终端里的 Vite，也不要单独结束 `tauri dev` 而留下另一个进程。
- **原因**：只停 Vite 或只停 Tauri 会导致白屏、数据加载失败等异常；必须保持前后端成对启停。
