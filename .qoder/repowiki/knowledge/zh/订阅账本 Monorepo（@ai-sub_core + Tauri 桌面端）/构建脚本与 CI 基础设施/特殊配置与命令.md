`npm run parity` 执行统计口径回归检查；`npm run parity -- --update` 接受当前输出并覆盖 `scripts/parity-baseline.json`；`PARITY_REF=YYYY-MM-DD npm run parity` 可仅打印报告而不比对基准；`./scripts/sign-macos-app.sh [IDENTITY]` 对已构建的 macOS App 签名，无参数时执行 ad-hoc 签名。