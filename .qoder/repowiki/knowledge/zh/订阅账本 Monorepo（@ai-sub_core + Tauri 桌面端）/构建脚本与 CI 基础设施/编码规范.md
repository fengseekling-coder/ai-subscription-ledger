- 脚本通过显式导入 `../packages/core/dist/index.js` 中的函数来消费 core 包，而非直接引用源码，确保只检测已编译产出的行为。
- 基准文件以纯 JSON 快照形式提交到仓库，脚本在首次缺失时提示用 `--update` 重建，避免硬编码期望值。
- CI job 使用 `continue-on-error: true` 明确标记非阻断性步骤（如安全审计），而关键构建与测试步骤保持严格失败语义。
- Shell 脚本统一使用 `set -euo pipefail` 开头，并通过 `$ROOT` 变量相对定位项目根目录，保证可移植性。