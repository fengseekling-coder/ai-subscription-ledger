- core 中与「今天」相关的函数统一接受可选 ref 参数，禁止直接调用 todayLocalISO/new Date()，以保证确定性测试
- 统计相关变更必须通过 `npm run parity` 验证，漂移需同步更新 baseline 或修正代码
- ESLint flat config 对 packages/core 与 apps/desktop 共享 TypeScript 规则，React Hooks 规则仅作用于 apps/desktop/src