文档本身无需构建，但其中引用的桌面端命令用于验证产品行为：`npm run test:core`（业务单测）、`npm run parity`（固定日期摘要对照）、`npm run tauri:build -w @ai-sub/desktop`（打包 macOS 应用）。macOS 签名需配置 `APPLE_SIGNING_IDENTITY`、`APPLE_ID`、`APPLE_PASSWORD`、`APPLE_TEAM_ID` 等环境变量，或使用仓库脚本 `scripts/sign-macos-app.sh`。