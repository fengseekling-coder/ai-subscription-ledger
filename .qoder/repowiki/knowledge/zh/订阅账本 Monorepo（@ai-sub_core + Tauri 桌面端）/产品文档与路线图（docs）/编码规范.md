- 文档采用「版本维护」表格结尾（含版本号、日期、变更说明），便于追踪演进历史。
- 产品边界以「做 / 不做」清单形式明确界定范围，避免功能蔓延。
- 数据模型与字段命名严格对齐 `@ai-sub/core` 中的 TypeScript 类型（如 `rows[]`、`bills[]`、`kind: payment | renewal`），文档即契约。
- 统计口径以表格形式列出指标名称、计算规则与对外话术，确保对内对外表述一致。
- 埋点事件名统一使用 `snake_case`，属性尽量精简且不采集敏感信息（订单号、备注全文、订阅 URL）。