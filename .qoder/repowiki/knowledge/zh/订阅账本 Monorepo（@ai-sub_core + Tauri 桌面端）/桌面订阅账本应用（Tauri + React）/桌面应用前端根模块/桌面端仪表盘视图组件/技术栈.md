React 函数组件 + TypeScript；样式采用 CSS 类名（BEM 风格，如 `metric__value`、`summary-strip__item`、`month-bars__fill`）；国际化通过本地 `i18n` 模块；状态管理依赖 `@ai-sub/core` 提供的纯函数与 `AppState`/`Summary`/`Bill`/`SubscriptionRow` 等类型。